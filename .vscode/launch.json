{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for XDebug",
            "type": "php",
            "request": "launch",
            "port": 9003,
            "pathMappings": {
				"/app/public/packages/plugins/curso-php-conference-2025": "${workspaceRoot}/plugin",
                "/app": "${workspaceRoot}"
            }
        }
    ]
}
